//Part 1

import React from 'react'

const UserContext = React.createContext()
/*createContext()  -- is a method in a react*/
// context is a provider that provides the variable so that's why UserContext is also a provider
export default UserContext;